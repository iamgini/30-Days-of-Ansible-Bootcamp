---
- name: Loop & Task Control
  hosts: localhost
  gather_facts: false
  vars_files:
    - secret-vars.yaml
  tasks:
    - name: Show Message
      ansible.builtin.debug:
        msg: "{{ secret_token }}"
